import Card from '../../common/Card/Card';

const SurveyReminder = () => {
  return (
    <Card title="Survey reminder">
      <p>Send reminder about survey</p>
    </Card>
  );
};

export default SurveyReminder;
