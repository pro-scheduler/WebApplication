import Card from '../../common/Card/Card';

const MeetingReminder = () => {
  return (
    <Card title="Meeting reminder">
      <p>Send reminder about the meeting to all participants</p>
    </Card>
  );
};

export default MeetingReminder;
